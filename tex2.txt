.clear()- удалят все из библиотеки
.get("*")-получить значение по *
.keys()-возвращает список всех ключей
.values()-возвращает список всех значение
.i temos()-возвращает список ключей и их значение
.update("*")-берет множество и обнавляает или дополнает к нему ключи из *


.add ("*")-добовлаеть элементу в множество
.update("*")-добовлаеть в множество все что вы передали в update()
.intersecton("*")-вл\озврщает обьекты которые есть и во множестве и в *
.remove("*")-удалят обьект в множества
.difference("*")-сравнивает оргинальное множество с * и возвращает обьекты которых нет в *
.intersecton_update(*)-удалят из множества непохожие и остоновляет только те обьекты ккоторые есть и во множестве и в *
.klear()-удаляет все внутри  множества
.discard("*")-удаляет * из множества, если * нету во множестве, ошибки не будет как  если бы это было remove()
.pop()-удаляет рандомный обьект из множеств.


tuple(*)-конвертатор tuple() конвертирует обьект в тип данных TUPLE.
set(*)- конвертатор set() конвертирует обьект в тип данных set. также позвщлает создать пустой SET.
list(*)- конвертатор list() конвертирует обьект в тип данных LIST.
dict(*)-конвертатор dict() конвертирует обьект в тип данных dict, не все обьекты могут быть конвертираваны!


.read()-данный метод выгружает все содержимое файла в переменную
.readlines()-данный метод берет каждую линию текста и добовляает ее в листу
.write(*)-данный методзаписьвает содержимое в файл.
.close()-данный метод ОБЯЗАТЕЛЕН!

abs(x)-
all(interable)-
any -
ascii(abject)
bin(x)
bool(x=false)
class bytearray
class baytearray(source)
encoding, errors









